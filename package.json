{
  "name": "studytrack-backend",
  "version": "1.0.0",
  "description": "Vercel serverless backend for StudyTrack — proxies Gemini 2.5 Flash API",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  }
}
